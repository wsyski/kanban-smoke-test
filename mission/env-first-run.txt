# Mission environment — Java word-count CLI

Records the board wiring of the first run. `file-mission.sh` recreates this
automatically; this file is the reference if you rebuild by hand or port the
scenario to another board.

BOARD=smoke-test
MISSION_REPO=/opt/projects/kanban-smoke-test/main/kanban-smoke-test
MISSION=Java word-count CLI
MODE=small-single-branch-stage-only
LANE=TW -> C1 -> RVa -> G2
BASELINE_SHA=f5465ae15159314b9ec2782d060a0127b98f6e04
IDEMPOTENCY_PREFIX=smokejava

# First run card ids (2026-09-05) — done, kept for provenance:
TW=t_11606aba    # tester   acceptance tests RED-first   patch: t_11606aba.patch
C1=t_6e737860    # coder    implementation GREEN         patch: t_6e737860.patch
RVA=t_f4d747dd   # reviewer PASS verdict
G2=t_5163381e    # human    commit 74274bb pushed

# Runtime wiring that made it work:
# - board default_workdir = MISSION_REPO (set via
#     hermes kanban boards set-default-workdir smoke-test <repo>)
#   but every card also pins --workspace dir:<repo> explicitly (belt+braces:
#   an omitted workspace silently becomes fresh scratch).
# - dispatch owner = the default-profile gateway (dispatch_in_gateway: true in
#     ~/.hermes/config.yaml); verify: lsof ~/.hermes/kanban/.dispatcher.lock
#     then `ps -p <pid> -o cmd=`.
# - dispatcher tick: 60s (kanban.dispatch_interval_seconds).
# - every root card is created on the human-gate lane (assignee human-gate),
#   blocked, and only unblocked+assigned at launch — a root with a real
#   assignee is ready immediately and the dispatcher may claim it mid-filing.
# - never put --goal on reviewer/gate cards: the judge can push a reviewer to
#   complete and silently open the gate it guards.
