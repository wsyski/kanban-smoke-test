HARD RULES — breaking any of these fails the card, whatever else you achieve:
(1) Do NOT commit, and do NOT create, switch, rename or delete branches — stay on the branch you find.
(2) Do NOT install packages or create virtualenvs. Toolchain: JDK 17 + Maven 3.9 on PATH; dependencies pre-warmed in ~/.m2. JUnit console jar (own unit tests only): /tmp/junit/junit-platform-console-standalone-1.10.2.jar.
(3) Never run git restore, git checkout --, git stash, git reset or git clean — earlier work is staged/committed; those commands destroy it.
(4) Stage ONLY files you touched (git add -- <paths>), never git add -A; never stage target/ or build output — extend .gitignore for target/ if needed.
(5) Do not write profile memories.
(6) When done: git diff --cached -- <your paths> > /tmp/<YOUR-CARD-ID>.patch and attach it as a kanban_complete artifact. The human commits at the deployment gate — leaving work staged is the whole point.

CONTEXT: Task 2 of 2, sequential after the word-count CLI (task 1, already committed — its files must keep working; do not modify them). The acceptance tests from the parent card are staged in the working tree (tests/run_wordcount_service.sh + fixtures) and currently RED for the right reason (no service jar).

TASK: Implement WordCountService — a minimal Spring Boot web service — so the black-box acceptance tests pass.

CONTRACT: mvn -q -DskipTests package -> runnable fat jar on JDK 17; service listens on port 8080; POST /count non-empty text body -> 200 with the word count (whitespace-separated tokens) as plain text; empty body -> 400; newline-only body -> 200 with '0'; multi-line text counts across lines; unknown paths -> normal 404.

YOU OWN: pom.xml (Spring Boot 3.x, Java 17, spring-boot-starter-web ONLY — no other starters/dependencies; spring-boot-maven-plugin for the fat jar), src/main/java/** (keep it minimal: one application class + one controller; word counting may reuse the same definition as task 1: whitespace-separated tokens), application.properties only if needed (default port 8080 is already right), .gitignore for target/.

DO NOT: add a database, security, actuator, docs, or any dependency beyond starter-web; modify task 1's files (WordCount.java, tests/run_count_words.sh, tests/fixtures/); implement the tester's script for them.

BEFORE COMPLETING: mvn -q -DskipTests package, then run bash tests/run_wordcount_service.sh — paste the GREEN summary (e.g. '5 passed, 0 failed'). Also run bash tests/run_count_words.sh — task 1 must still pass (paste its line too). If an acceptance test is wrong, do NOT silently weaken it — report the conflict in your result instead.

Stage only your files. Dispatch subagents for parallelizable or context-heavy work; keep only distilled results in your own context.
