GATE (human in normal runs; script in --auto-gates mode — never a worker).

Authorize the CODE. Before completing:
1. Reviewer verdict of this task's final review (parent card) is PASS — check the parent's result.
2. All suites GREEN from a clean build (mvn -q test / verify as applicable).
3. The staged index contains ALL of this task's files and nothing else. Review the staged diff: git diff --cached --stat.
4. YOU commit: git commit -m "task <N>: <one line>" — the flow itself never commits; the commit is the gate-holder's act.
5. Complete with the commit SHA in the result. This commit is what unlocks the next task's root card.

Never commit anything except this task's staged files.
