HARD RULES — breaking any of these fails the card, whatever else you achieve:
(1) Do NOT commit, and do NOT create, switch, rename or delete branches — stay on the branch you find.
(2) Do NOT install packages or create virtualenvs. Toolchain: JDK 17 + Maven 3.9 on PATH; dependencies are pre-warmed in ~/.m2 (offline build must work; never add new dependencies beyond spring-boot-starter-web without recording why in your result). JUnit console jar (own unit tests only): /tmp/junit/junit-platform-console-standalone-1.10.2.jar.
(3) Never run git restore, git checkout --, git stash, git reset or git clean — earlier missions' work (word-count CLI + its tests) is staged or committed; those commands destroy it.
(4) Stage ONLY files you touched (git add -- <paths>), never git add -A; never stage target/ or build output.
(5) Do not write profile memories.
(6) When done: git diff --cached -- <your paths> > /tmp/<YOUR-CARD-ID>.patch and attach it as a kanban_complete artifact. The human commits at the deployment gate — leaving work staged is the whole point.

CONTEXT: This is task 2 of 2 in a sequential mission on this board. Task 1 (word-count CLI, committed) is done; its files (WordCount.java, tests/run_count_words.sh, tests/fixtures/) must keep working — do not modify them. The parent of this card's root is task 1's gate, so this task only started after task 1 was committed.

TASK: Write BLACK-BOX acceptance tests for 'WordCountService' — a Spring Boot web service exposing the word count over HTTP — asserting observable behavior only (HTTP status + response body). Do NOT write unit tests and do NOT assume internal structure: the coder picks the implementation and owns its unit tests. Each test must fail against the current code for the right reason — run them and paste the RED output; a test that cannot fail proves nothing. Complete with a task-to-test mapping.

CONTRACT the coder must satisfy; tests assert only this:
- Build: mvn -q -DskipTests package produces a runnable fat jar (spring-boot-maven-plugin repackage).
- Run: java -jar target/<name>.jar listens on port 8080.
- POST /count with a non-empty plain-text body -> HTTP 200, response body = the number of words (whitespace-separated tokens) as plain text, e.g. '2'.
- POST /count with an empty body -> HTTP 400 (client error), not a crash.
- Multi-line body counts words across lines: 'one two\nthree four' -> '4'.
- Newline-only body ('   \n  ') is NOT a 400 — it is valid text with 0 words -> '0'.
- GET on unknown paths behaves like a normal 404 (no crash).

TEST HARNESS: tests/run_wordcount_service.sh at repo root — plain bash + curl, no pytest, no build tools beyond maven itself. Behavior: (1) locate the jar under target/ (glob *.jar, excluding *-sources/original); if none, every check FAILs with reason 'service jar not built — run mvn package' and the script exits non-zero (this is the required RED phase: nothing exists yet). (2) If a jar exists, start it on port 8080 (fail fast with a clear message if 8080 is already occupied), wait until the port answers (max ~30s), run each check via curl -s -o /dev/null -w '%{http_code}' and curl -s for bodies, then ALWAYS kill the service (trap). (3) One PASS/FAIL line per acceptance criterion; final line 'N passed, M failed'; non-zero exit if any FAIL.

Fixtures: create them inline in the script (printf 'one two\nthree four' > tmpfile etc.) or under tests/fixtures-service/ — your choice; keep everything inside tests/ + tmpfiles. Do not implement or fix production code. Do not touch task 1's files. Dispatch subagents for parallelizable or context-heavy work; keep only distilled results in your own context.
