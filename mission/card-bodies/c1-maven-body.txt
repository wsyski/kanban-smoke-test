HARD RULES — breaking any of these fails the card, whatever else you achieve:
(1) Do NOT commit, and do NOT create, switch, rename or delete branches — stay on the branch you find.
(2) Do NOT install packages or create virtualenvs. Toolchain: JDK 17 + Maven 3.9 on PATH (~/.m2 pre-warmed). JUnit console jar (own unit tests only): /tmp/junit/junit-platform-console-standalone-1.10.2.jar.
(3) Never run git restore, git checkout --, git stash, git reset or git clean — earlier missions' work is staged/committed; those commands destroy it.
(4) Stage ONLY files you touched (git add -- <paths>), never git add -A; never stage target/ or build output.
(5) Do not write profile memories.
(6) When done: git diff --cached -- <your paths> > /tmp/<YOUR-CARD-ID>.patch and attach it as a kanban_complete artifact. The human commits at the deployment gate.

CONTEXT: Task 3 of 3 (sequential, after the WordCountService mission — its gate is this card's parent). Mission 1's word-count CLI (committed as WordCount.java + tests/run_count_words.sh) currently builds with plain javac. The user requires Maven as the build tool for EVERY task in this repo.

TASK: Convert the word-count CLI to a Maven build WITHOUT changing its observable behavior or its acceptance-test semantics.

CONTRACT (unchanged CLI behavior): WordCount reads a UTF-8 text file, prints ONE line: number of whitespace-separated words, exit 0; empty/whitespace-only file prints 0; missing file -> stderr message, exit 2.

REQUIRED CHANGES:
- pom.xml at repo root: Maven jar project, Java 17, artifactId wordcount-cli, NO dependencies beyond what is already there (nothing — keep it dependency-free), NO shade/spring plugins. 'mvn -q -DskipTests compile' must produce target/classes/WordCount.class.
- Move WordCount.java to src/main/java/WordCount.java (default package — do NOT add a package declaration; the run line stays 'java -cp target/classes WordCount').
- Delete the root-level WordCount.java (its move is the deletion; stage accordingly).
- Update tests/run_count_words.sh: replace the javac build step with 'mvn -q -DskipTests compile' and run 'java -cp target/classes WordCount <file>'. All 5 checks and their expected outputs MUST remain byte-identical — if you find yourself changing an expectation, stop and report instead.
- Extend .gitignore for target/ if not covered.

BEFORE COMPLETING: mvn -q -DskipTests compile, then bash tests/run_count_words.sh — paste the GREEN summary ('5 passed, 0 failed'). The moved source must compile with no behavior change.

If tests/run_wordcount_service.sh or other mission-2 files exist, do NOT touch them. Stage only your files. Dispatch subagents for parallelizable or context-heavy work; keep only distilled results in your own context.
