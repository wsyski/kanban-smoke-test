HARD RULES — breaking any of these fails the card, whatever else you achieve:
(1) Do NOT commit, and do NOT create, switch, rename or delete branches — stay on the branch you find.
(2) Do NOT install packages. Toolchain ready: mvn 3.9 + Java 17 (~/.m2 pre-warmed for Boot 3.3.x + openapi-generator 7.x).
(3) Never run git restore, git checkout --, git stash, git reset or git clean.
(4) Stage ONLY your own files (git add -- <paths>), never git add -A; never stage target/.
(5) Do not write profile memories.
(6) When done: git diff --cached -- <your paths> > /tmp/<YOUR-CARD-ID>.patch, attach, evidence in result field.

TASK (task 1 — wordcount-cli/): Implement per plan task-1 so TW1's staged suite goes GREEN:
- pom.xml: Spring Boot 3.3.x parent, starter only (spring-boot-starter), spring-boot-maven-plugin repackage; fat jar executable.
- App class + CommandLineRunner: read all stdin bytes (UTF-8), tokenize on whitespace (regex \\s+ on the decoded string, or stream tokenizer), print count + newline, exit 0.
- Make EXACTLY TW1's tests pass — add nothing the tests don't require (YAGNI). Do not modify TW1's test files.
- Verify GREEN: mvn -q test → all pass; paste summary in result. Attach patch (pom + src/main only).
