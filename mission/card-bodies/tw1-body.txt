HARD RULES — breaking any of these fails the card, whatever else you achieve:
(1) Do NOT commit, and do NOT create, switch, rename or delete branches — stay on the branch you find.
(2) Do NOT install packages or create virtualenvs. Toolchain ready: mvn 3.9 + Java 17 (~/.m2 pre-warmed).
(3) Never run git restore, git checkout --, git stash, git reset or git clean — other cards' staged work is unrecoverable.
(4) Stage ONLY files you touched (git add -- <paths>), never git add -A; never stage target/ or build output.
(5) Do not write profile memories.
(6) When done: git diff --cached -- <your paths> > /tmp/<YOUR-CARD-ID>.patch, attach as artifact, evidence in the result field (hermes kanban CLI).

TASK (task 1 — wordcount-cli/): Write the JUnit 5 UNIT tests FIRST, per plan task-1 (mission/plans/task-1-plan.md):
- Counting tests: words = maximal runs of non-whitespace; cases: empty input→0, single word, multiple words, multiple whitespace kinds (spaces/tabs/newlines), leading/trailing whitespace, UTF-8 multibyte (e.g. 'öAKrug hörbar' → 2), large input (400+ words) count exact.
- Entry-point tests: feed bytes via captured System.in, assert stdout bytes + exit 0 (redirect junit to /dev/null).
- Where: wordcount-cli/src/test/java/dev/kanban/wordcount/cli/. pom.xml may be created/staged ONLY if the plan scaffolds it in this card — otherwise it is the coder's file, and you write tests against the plan's declared interfaces.
- Verify RED: run the suite; every test must FAIL (class missing). Paste summary in the result. Attach patch.
