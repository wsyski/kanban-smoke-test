HARD RULES — breaking any of these fails the card, whatever else you achieve:
(1) Do NOT commit, and do NOT create, switch, rename or delete branches — stay on the branch you find.
(2) Do NOT install packages or create virtualenvs. Toolchain is ready: javac/java 17 on PATH; JUnit console jar (optional, for unit tests only) at /tmp/junit/junit-platform-console-standalone-1.10.2.jar.
(3) Never run git restore, git checkout --, git stash, git reset or git clean — earlier cards' work (the acceptance tests) is staged and uncommitted; those destroy it unrecoverably.
(4) Stage ONLY files you touched (git add -- <paths>), never git add -A; never stage build/ output.
(5) Do not write profile memories.
(6) When done: git diff --cached -- <your paths> > /tmp/<YOUR-CARD-ID>.patch and attach it as a kanban_complete artifact. The human commits at the deployment gate — leaving work staged is the whole point.

TASK: Implement the Java word-count program so the black-box acceptance tests staged by the parent card pass. First read the parent card and its comments (the tester documented the RED run and mapping). The tests live in tests/run_count_words.sh + tests/fixtures/ (already in the working tree, staged).

CONTRACT (must hold exactly): java -cp build WordCount <file> — prints one line: word count (whitespace-separated tokens) to stdout, exit 0; empty or whitespace-only file prints 0; missing file prints a message to stderr and exits 2.

YOU OWN: WordCount.java (repo root or src/, your choice), the build step (plain javac only — 'javac -d build <sources>' from repo root, no Maven/Gradle), a .gitignore for build/, and minimal unit tests for internals if you deem them worthwhile (plain java, optional — skip if the class is a single main()).

BEFORE COMPLETING: build, then run bash tests/run_count_words.sh — paste the GREEN summary (e.g. '4 passed, 0 failed'). All acceptance checks must pass for the right reasons; if a test is wrong, do NOT silently weaken it — report the conflict in your result instead.

Stage only your files. Dispatch subagents for parallelizable or context-heavy work; keep only distilled results in your own context.
