You are the integration tester. Interfaces already exist and were reviewed (parent RVa = PASS). You write END-TO-END integration tests against the REAL assembled system, then run every suite in the repo. Hard rules first:

HARD RULES — breaking any of these fails the card, whatever else you achieve:
(1) Do NOT commit, and do NOT create, switch, rename or delete branches — stay on the branch you find.
(2) Do NOT install packages or create virtualenvs. Toolchain: JDK 17 + Maven 3.9 on PATH (~/.m2 pre-warmed). bash + curl for the e2e script.
(3) Never run git restore, git checkout --, git stash, git reset or git clean — earlier work is staged/committed; those commands destroy it.
(4) Stage ONLY files you touched (git add -- <paths>), never git add -A; never stage target/ or build output.
(5) Do not write profile memories.
(6) When done: git diff --cached -- <your paths> > /tmp/<YOUR-CARD-ID>.patch and attach it as a kanban_complete artifact.

CONTEXT: Task 2 of 2 (Spring Boot WordCountService, POST /count -> word count; empty body -> 400; port 8080). Task 1 = word-count CLI (WordCount.java, tests/run_count_words.sh) — must keep passing. Existing suites: tests/run_count_words.sh (task 1), tests/run_wordcount_service.sh (task 2 acceptance). Your job: the layer those do NOT cover.

TASK A — END-TO-END INTEGRATION TEST, tests/run_wordcount_service_e2e.sh at repo root (bash + curl, self-contained):
- Full lifecycle: mvn -q -DskipTests package -> locate fat jar under target/ -> start it as a real process on port 8080 (clear failure if port taken; max ~30s startup wait; ALWAYS kill on exit via trap) -> exercise over HTTP -> kill -> report.
- E2E scenarios (assert EXACT expected outputs, not just status codes): repeated sequential POSTs return consistent counts (e.g. same body three times -> same number every time); body with tabs/multiple spaces counts tokens not characters; large body (a few hundred words, generated inline) -> correct count; binary-safe: body with UTF-8 multibyte characters counts words correctly; Content-Type variations (text/plain and no content-type header) both count correctly; POST /count right after boot (no warmup) works.
- Format: one PASS/FAIL line per scenario; final line 'N passed, M failed'; non-zero exit on any FAIL; service process MUST be gone after the script exits (verify with a port check, fail loudly if you leaked it).

TASK B — FULL SUITE RUN (the mission's integration gate): run ALL suites in order and print one combined summary:
  bash tests/run_count_words.sh
  bash tests/run_wordcount_service.sh
  bash tests/run_wordcount_service_e2e.sh
All must be GREEN. Paste each suite's final line in your completion result. If an EARLIER suite fails, that is a defect in earlier cards' work: do NOT fix their code — report the failure in your result (the reviewer/gate handles it). Only your own script is yours to fix.

Stage only your file. Dispatch subagents for parallelizable or context-heavy work; keep only distilled results in your own context.
